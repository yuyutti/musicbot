//const language = require('../lang/commands/point');

module.exports = {
    data: {
        name: 'point',
        description: 'Display current points for intro quiz game',
        name_localizations: {
            ja: 'point',
        },
        description_localizations: {
            ja: 'イントロドン: ポイント表示',
        }
    },
    async execute(interactionOrMessage, args, lang) {
        // ポイント表示処理を記入
    },
}
